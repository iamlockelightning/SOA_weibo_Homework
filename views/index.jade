extends layout

block content
  .container
    form.form-signin
      h2.form-signin-heading Please sign in
      button.btn.btn-lg.btn-primary.btn-block(id = 'signin', type='submit') Sign in
    br
    br
    p -/users：返回所有注册用户
    p -/posts/'<'uid'>'：返回用户的最近100条微博
    p -/emotion/'<'uid'>'：返回用户心情
